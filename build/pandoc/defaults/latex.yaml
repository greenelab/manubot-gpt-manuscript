# Pandoc --defaults for LaTeX output.
# Load on top of common defaults.
from: markdown-auto_identifiers
to: latex
output-file: latex/manuscript.tex
standalone: true
variables:
  # papersize: a4
  geometry:
  - top=1in
  - left=0.5in
  - right=0.5in
  - bottom=1in
